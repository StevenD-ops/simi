import ts from "typescript";
import { NodeParser } from "../NodeParser";
import { Context } from "../NodeParser";
import { SubNodeParser } from "../SubNodeParser";
import { ObjectType } from "./../Type/ObjectType";
export declare class FunctionParser implements SubNodeParser {
    private childNodeParser;
    constructor(childNodeParser: NodeParser);
    supportsNode(node: ts.ArrowFunction): boolean;
    createType(node: ts.FunctionTypeNode | ts.ArrowFunction, context: Context): ObjectType | undefined;
}
